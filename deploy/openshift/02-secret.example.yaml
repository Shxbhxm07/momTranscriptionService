# TEMPLATE — fill in and apply, never commit the filled copy.
#   oc create secret generic mom-secrets --from-env-file=secrets.env
apiVersion: v1
kind: Secret
metadata:
  name: mom-secrets
type: Opaque
stringData:
  CP4D_USERNAME: "<cp4d-user>"
  CP4D_API_KEY: "<cp4d-api-key>"
  GROQ_API_KEYS: "unused"          # legacy variable name; must be non-empty for the service to start
  MINIO_ACCESS_KEY: "<minio-access-key>"
  MINIO_SECRET_KEY: "<minio-secret-key>"
  ELASTIC_USER: "<elastic-user>"
  ELASTIC_PASSWORD: "<elastic-password>"
