# Whisper + NeMo as pods, for a cluster with NVIDIA GPUs. Left out by the split overlay, where both
# run on the GB10 and OCP only calls them over the LAN.
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - pvc-models.yaml
  - whisper-server.yaml
  - nemo-service.yaml
